clear;
n= 30000;
k=6;

ficheiro = 'Dados_Jogadores.txt';
data = readcell(ficheiro,'Delimiter',',');

jogadores= data(2:end,1);
classes= data(2:end,9);

BF = zeros(1,n,'uint8');

for nj = 1:length(jogadores)
    
    if classes(nj)=='Suspeito'
        BF = adicionarElemento(,BF,k);
    end
end
%{
BF = zeros(1,n,'uint8');

for 
BF = adicionarElemento(elemento,BF,k);

x='Aveiro';
r= verificarElemento(x,BF,k)
%}